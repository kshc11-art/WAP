# AGENTS.md

이 저장소의 코드 리뷰 규칙입니다. (Codex 등 리뷰 에이전트용)

## 배경

- `userscripts/`는 **사내 업무 시스템 위에서 실제로 매일 돌아가는** Tampermonkey 스크립트입니다.
  잘못되면 사내 페이지가 깨지고 업무가 멈춥니다. 리뷰의 1순위는 "기존 동작을 해치지 않는가"입니다.
- 작성자는 사내망 밖에서 개발하고, 최종 검증은 회사 PC에서 수동으로 합니다.
  따라서 **런타임에서 드러날 문제를 코드만 보고 잡아내는 것**이 리뷰의 핵심 가치입니다.
- 코딩·보안 규칙의 본문은 [ENGINEERING_RULES.md](ENGINEERING_RULES.md)에 있습니다. 그 기준으로 봐 주세요.

## 반드시 확인할 것

**안전성**
- DOM 요소가 없을 때 오류 없이 종료되는가? (`querySelector` 결과 null 체크)
- API 응답이 비었거나(`[]`, `null`) 필드가 없거나 형식이 바뀌었을 때 **기존 페이지 동작이 유지**되는가?
- 최상위 진입점에 `try/catch`가 있는가? 예외가 사내 페이지 스크립트로 전파되지 않는가?
- 사내 시스템의 기존 함수·이벤트 핸들러·전역 객체를 **덮어쓰지** 않는가?
- 이 기능을 끌 수 있는가? (플래그 또는 스크립트 비활성화만으로 원상복구)

**행위 제한**
- 사용자 확인 없이 **쓰기 API**(save/create/update/delete/approve)를 호출하지 않는가?
- 자동 클릭·자동 제출로 사실상 결재/저장을 실행해 버리는 흐름이 없는가?
- 반복 호출·전체 조회가 서버에 과한 부하를 주지 않는가? (기간 조건 없는 전체 조회 주의)

**보안**
- 실제 개인정보, 사번, 특허·과제 데이터, 금액이 코드·주석·테스트에 들어있지 않은가?
- 쿠키·세션·`Authorization` 헤더·비밀번호·API 키가 하드코딩되어 있지 않은가?
- 커밋된 fixture가 익명화되어 있는가?

**호환성**
- 같은 페이지에서 도는 **다른 userscript와 충돌**할 여지가 없는가?
  (전역 변수명, `id`/`class` 이름, 단축키 키 조합, `@match` 범위 중복)
- 상태 코드 해석을 화면 구분 없이 뭉뚱그리지 않았는가? (같은 코드값이 탭마다 의미가 다름)
- 원본 상태값이 정규화 과정에서 버려지지 않았는가?

**형식**
- userscript를 고쳤으면 `// @version`이 올라갔는가?
- 변경 범위가 Issue 하나에 대응하는가? (관계없는 리팩터링이 섞였으면 지적)

## 리뷰 톤

- 실제로 깨질 수 있는 시나리오를 **구체적인 입력/상황으로** 지적해 주세요.
  ("응답의 `list`가 `null`이면 3번째 줄에서 예외" 처럼)
- 스타일 취향 지적보다 위 항목을 우선합니다.
- 사내망에서 사람이 확인해야만 알 수 있는 부분은 **"내부망 확인 필요"로 표시**하고 넘어가 주세요.
