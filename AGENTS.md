# AGENTS.md

이 저장소의 코드 리뷰 규칙이다. (Codex 등 PR 리뷰 에이전트용)

규칙의 원본은 [ENGINEERING_RULES.md](ENGINEERING_RULES.md)이고, 사실의 원본은 `contracts/`다.
그 기준으로 봐 주면 된다.

---

## 배경

- `scripts/`의 파일은 **사내 업무 시스템 위에서 매일 실제로 도는** Tampermonkey 스크립트다.
  잘못되면 사내 페이지가 깨지고 업무가 멈춘다. 리뷰 1순위는 "기존 동작을 해치지 않는가"다.
- 작성자는 사내망 밖에서 개발하고 최종 검증은 회사 PC에서 수동으로 한다.
  **런타임에서야 드러날 문제를 코드만 보고 잡아내는 것**이 리뷰의 핵심 가치다.
- 이 스크립트들은 **외부 AI 채팅에서 파일 전문으로 다시 쓰여 돌아온다.**
  따라서 diff가 "6,000줄 삭제 / 8,000줄 추가"인 PR이 정상적으로 발생한다.

---

## PR을 두 종류로 나눠서 본다

| | 판정 | 리뷰 방식 |
|---|---|---|
| **부분 수정** | 변경량이 원본의 30% 미만 | 아래 체크리스트로 diff를 본다 |
| **전면 교체** | 30% 이상, 또는 섹션 신설·삭제 | **줄 단위 리뷰를 시도하지 마라.** 아래 "전면 교체" 항목만 본다 |

전면 교체 PR에서 줄 단위 지적은 신호 대 잡음비가 0이다. 형식적 통과를 만들 뿐이다.

### 전면 교체 PR에서 볼 것 — 이것만

1. `/* WAP-ID START … END */` 프리앰블이 살아 있는가
2. `@namespace`가 `contracts/scripts.yaml`의 `target`과 같은가
3. `/* WAP-INLINE … */` 마커 쌍의 균형이 맞는가
4. 파일이 중간에 잘리지 않았는가 (마지막 줄, 괄호 균형)
5. `@version`이 올라갔는가
6. 새 전역·새 단축키·새 z-index·새 엔드포인트가 도입됐는가
7. 실명·사번·사내메일·실제 관리번호가 유입됐는가
8. 외부 CDN `@require`가 추가됐는가

---

## 부분 수정 PR 체크리스트

**안전성**
- DOM 요소가 없을 때 오류 없이 종료되는가? (`querySelector` 결과 null 체크)
- 응답이 비었거나(`[]`, `null`) 필드가 없거나 형식이 바뀌었을 때 **기존 페이지 동작이 유지**되는가?
- 응답 봉투의 `errorCode`를 확인하는가? **HTTP 200만 보고 성공으로 판정하지 않는가?**
  (실측 사고: 다른 스크립트의 네트워크 훅이 차단 응답을 200으로 돌려줘 가짜 성공이 발생한 사례가 있다.)
- 최상위 `try/catch`가 있고 예외가 사내 페이지로 전파되지 않는가?
- 사내 시스템의 기존 함수·이벤트·전역을 **덮어쓰지** 않는가?

**행위 제한**
- 사용자 확인 없이 쓰기 API(save/create/update/delete/approve)를 호출하지 않는가?
- **확인 다이얼로그를 자동으로 클릭하지 않는가?** 한다면 그것을 끄는 킬스위치가 같은 파일에 있는가?
- 차단 정규식을 파일 안에 따로 두지 않고 `contracts/write-guard.yaml`을 따르는가?
- 기간 조건 없는 전체 조회로 서버에 부하를 주지 않는가?

**정체성·충돌**
- 삽입 요소에 `data-wap="<slug>"` 표식이 붙는가?
- DOM 접두어·저장키·단축키·z-index가 `contracts/scripts.yaml`의 `owns` 범위 안인가?
- 같은 페이지에 함께 뜨는 스크립트(→ `state/pages.md`)와 전역 이름이 겹치지 않는가?
- 잃으면 곤란한 상태를 `GM_setValue`에 두지 않았는가? (`localStorage`/IndexedDB로 가야 한다)

**데이터 판정**
- 상태코드를 전역 공통표로 해석하지 않는가? **화면(탭)별로 구분하는가?**
- 원본 상태값이 정규화 과정에서 버려지지 않았는가?
- 계약에 없는 엔드포인트·상태코드를 코드에 직접 박지 않았는가?

**보안**
- 실명·사번·사내메일·실제 관리번호·금액이 코드·주석·테스트에 있는가?
  `\uXXXX` 이스케이프 안에 숨어 있을 수 있다.
- 쿠키·세션·인증 헤더·API 키가 하드코딩돼 있는가?

---

## 리뷰 톤

- **구체적인 입력과 상황으로** 지적해라. ("응답의 `data`가 `null`이면 3번째 줄에서 예외")
- 스타일 취향보다 위 항목을 우선한다.
- 사내망에서 사람이 확인해야만 알 수 있는 것은 **"내부망 확인 필요"로 표시**하고 넘어간다.
- 지적이 계속 새로 생겨 수렴하지 않으면, 개별 지적을 늘리지 말고
  **"무엇이 아직 해결되지 않았는지"를 한 번에 정리**해라.
