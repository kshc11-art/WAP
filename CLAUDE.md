# CLAUDE.md

## 이 저장소

개인 업무 자동화 저장소입니다. 두 축이 있습니다.

1. **`userscripts/`** — 사내 웹 시스템(포털 / IPMS / 메일) 위에서 도는 Tampermonkey 스크립트.
   이미 실제 업무에 매일 쓰이고 있습니다. **깨지면 그날 업무가 막힙니다.**
2. **`python/`** — 엑셀 대사·통계·보고서 자동화. 브라우저에서 내보낸 JSON + 기존 Excel을 입력으로 받습니다.

**너는 사내망에 접속할 수 없습니다.** 실제 API도, 실제 데이터도 볼 수 없습니다.
검증은 최종적으로 사람이 회사 PC에서 합니다. 그러니 "돌려보니 됩니다"라고 말하지 말고,
**무엇을 확인해야 하는지**를 알려주세요.

## 반드시 지킬 것

**[ENGINEERING_RULES.md](ENGINEERING_RULES.md)를 먼저 읽고 그대로 따릅니다.** 특히:

- 실제 업무 데이터·인증정보를 코드나 테스트에 넣지 않습니다. (§1)
- 조회 API만 씁니다. 저장/승인/삭제를 자동 실행하지 않습니다. (§2)
- DOM이 없거나 API 응답이 이상하면 **조용히 종료하고 사내 페이지 원래 동작을 유지**합니다. (§3)
- `main`에서 직접 작업하지 않습니다. 한 번에 Issue 하나만 처리합니다. (§5)

## 작업 순서

1. Issue 번호를 확인하고, **그 Issue에 적힌 범위만** 합니다.
2. 고칠 파일의 **현재 코드를 먼저 읽습니다.** 지금 어떻게 동작하는지 요약할 수 있어야 시작합니다.
3. 브랜치를 만듭니다: `feature/…` `fix/…` `refactor/…`
4. 구현합니다. 새 기능은 **끄기 쉽게**(상단 플래그 하나) 만듭니다.
5. userscript를 고쳤으면 `// @version`을 올립니다.
6. 판정·계산 로직을 건드렸으면 `tests/fixtures/sanitized/`의 익명 데이터로 테스트를 붙입니다.
7. commit → push → PR.

## PR에 반드시 쓸 것

- 무엇을 바꿨는지 (파일 단위)
- **영향 범위**: 이 변경으로 깨질 수 있는 기존 동작
- **되돌리는 법**: 문제가 생기면 어떻게 원복하는지
- **내부망에서 확인해야 할 항목**: 사람이 회사 PC에서 눌러볼 체크리스트
  (예: "IPMS 업무요청 탭 진입 → 카드 표시 → 건수가 탭의 목록 건수와 같은지")

## 하지 말 것

- 요청하지 않은 대규모 리팩터링. 잘 돌아가는 스크립트를 구조 때문에 갈아엎지 않습니다.
- 여러 Issue를 한 PR에 섞기.
- 실제 사내 URL·응답 예시를 문서나 테스트에 그대로 붙여넣기.
- 확인 없이 "테스트 통과"라고 단정하기. 사내망 동작은 사람이 확인해야만 확정됩니다.

## 참고

- 스크립트 목록·구조: [README.md](README.md)
- 저장소 운영 방식(브랜치·PR·라벨·머지 기준): [docs/GITHUB_OPERATIONS.md](docs/GITHUB_OPERATIONS.md)
