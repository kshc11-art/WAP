# CLAUDE.md

## 이 저장소

사내 웹 시스템(포털 / IPMS / 그룹웨어 메일) 위에서 도는 Tampermonkey 스크립트 17개
(총 35,659줄, 가장 큰 파일 6,505줄)와 연계 Python 자동화를 관리한다.

**정본은 이 저장소가 아니다.** 회사 PC의 Tampermonkey에서 지금 도는 코드가 정본이고,
저장소는 사본을 받아 정규화·색인·검사한다.
그래서 저장소는 **막지 않고 검사만** 한다. → [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

**너는 사내망에 접속할 수 없다.** 실제 API도 실제 데이터도 볼 수 없다.
검증은 사람이 회사 PC에서 한다. "돌려보니 됩니다"라고 말하지 말고
**무엇을 확인해야 하는지**를 알려줘라.

---

## 네 역할

이 저장소는 Claude / GPT / 사람이 나눠 쓴다. → [docs/AI_OPERATIONS.md](docs/AI_OPERATIONS.md)

**너가 맡는 것**
- 저장소 구조·계약 (`contracts/`, `rules/`)
- 검사기·자동화 (`tools/`, `.github/workflows/`, `state/` 생성기)
- 여러 스크립트에 걸친 진단 (충돌, 상태코드 분열, 중복)
- 반입본 리뷰와 위험 판정
- `python/`, `lib/` 원본, 매핑 설계

**너가 맡지 않는 것 — `scripts/` 안의 코드 파일**
사용자는 외부 AI 채팅에서 그 파일들을 계속 고치고 있다.
네가 직접 고치면 사용자의 작업본과 충돌하고, 충돌하면 저장소가 버려진다.
요청받았다면 **먼저 그 충돌 위험을 말하고 확인을 받아라.**

**거대 파일을 볼 때**는 전체를 읽지 말고 `state/outline/<slug>.md`(섹션 지도)로 접근해
필요한 구간만 부분 읽기 해라.

---

## 반드시 지킬 것

[ENGINEERING_RULES.md](ENGINEERING_RULES.md)를 먼저 읽고 그대로 따른다. 특히:

- **커밋을 막는 것은 정확히 4개**(§0). 검사 항목을 늘리자고 제안하기 전에 이 조항을 다시 읽어라.
- **검사기는 침묵을 성공으로 읽지 않는다**(§0). 검사 대상 0개는 실패다.
  이 저장소는 실제로 이 버그를 두 번 겪었다 — 한글 파일명, 글롭 미매칭.
- 실제 업무 데이터·인증정보를 코드나 문서에 넣지 않는다(§1).
- 조회 우선. 쓰기 API는 명시적 요청이 있을 때만(§2).
- DOM이 없거나 응답이 이상하면 조용히 종료하고 사내 페이지 동작을 유지한다(§3).
- **상태코드를 전역 공통표로 해석하지 않는다**(§4). 같은 코드가 화면마다 뜻이 다르다.
- `@namespace`는 slug로 고정. 정체성 키는 파일명이 아니라 slug다(§5).
- 외부 CDN `@require` 금지(§6).

---

## 작업 순서

1. Issue 번호를 확인하고 **그 범위만** 한다.
2. **현재 코드를 먼저 읽는다.** 지금 어떻게 동작하는지 요약할 수 있어야 시작한다.
3. 브랜치를 만든다: `feature/…` `fix/…` `refactor/…` `docs/…`
4. 구현한다.
5. 계약(`contracts/`)을 바꿨으면 `confidence`와 `verifiedAt`을 갱신한다.
   **모순은 해결하려 하지 말고 `conflict`로 드러내라.**
6. 판정·계산 로직을 건드렸으면 `contracts/fixtures/`의 익명 데이터로 테스트를 붙인다.
7. commit → push → PR.

---

## PR에 반드시 쓸 것

- 무엇을 바꿨는지 (파일 단위)
- **영향 범위** — 깨질 수 있는 기존 동작
- **되돌리는 법**
- **내부망 확인 체크리스트** — 사람이 회사 PC에서 눌러볼 항목

---

## 하지 말 것

- 요청하지 않은 대규모 리팩터링.
- 여러 Issue를 한 PR에 섞기.
- 실제 사내 응답 예시를 문서나 테스트에 붙여넣기.
- 확인 없이 "테스트 통과"라고 단정하기.
- **추정을 사실처럼 쓰기.** 확인 못 한 것은 `inferred`이거나 "확인 필요"다.
