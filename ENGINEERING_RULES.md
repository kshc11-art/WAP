# ENGINEERING RULES

이 저장소에서 코드를 쓰는 모든 주체(나, Claude Code, Codex)가 공통으로 따르는 규칙입니다.
`CLAUDE.md`와 `AGENTS.md`는 이 파일을 참조합니다. **규칙이 바뀌면 이 파일만 고칩니다.**

---

## 1. 보안 — 절대 commit 금지 (최우선)

아래는 어떤 이유로도 저장소에 들어가지 않습니다.

- 실제 로그인 쿠키 / 세션 / `Authorization` 헤더 / 비밀번호 / API 키
- 실제 사번·이름·연락처 등 개인정보
- 실제 특허·과제·비용 데이터 (Excel 원본, CSV, 조회 결과)
- 사내 API의 **전체 응답 dump**, 브라우저 HAR 파일, 네트워크 캡처
- 개인정보가 찍힌 스크린샷
- 회사 내부 문서 원본, 백업 zip

**테스트가 필요하면 익명화한 fixture를 씁니다** (`tests/fixtures/sanitized/`).

| 실제 값 | fixture 값 |
|---|---|
| `P260075KR` | `TEST0001KR` |
| 홍길동 | `USER_A` |
| 실제 금액 | 임의 금액 |
| 실제 요청번호 | `REQ001` |

내부 URL·API 경로 자체의 외부 반출 가능 여부는 **회사 보안정책 기준으로 내가 판단**합니다.
판단이 서기 전까지는 경로를 상수로 분리하고, 필요하면 `local/` 또는 로컬 설정으로 뺍니다.
(`local/`은 `.gitignore` 대상입니다.)

## 2. 사내 시스템에 대한 행동 제한

1. **조회(read) API 우선.** 목록·상세 조회만으로 목적을 달성할 수 있으면 그렇게 합니다.
2. **쓰기 API 금지.** 사용자가 명시적으로 요청하지 않은 `save/create/update/delete/approve` 호출은 하지 않습니다.
3. **실제 저장·승인·결재·삭제의 자동 실행 금지.** 값을 채워주는 것까지가 한계이고, 최종 실행 버튼은 사람이 누릅니다.
4. 반복 호출은 사람이 화면을 쓰는 속도를 크게 넘지 않게 합니다. 대량 조회는 페이지 단위로 나눠서 요청합니다.

## 3. 방어적 코딩 (기존 시스템을 절대 깨뜨리지 않기)

- **DOM 미존재 시 정상 종료.** 대상 요소가 없으면 오류 없이 조용히 끝냅니다.
- **시스템 기능을 덮어쓰지 않습니다.** 기존 함수·이벤트를 교체하지 말고 추가합니다.
- **API 응답이 예상과 다르면 기존 동작을 유지**하고, 내 기능만 비활성화합니다.
- 모든 진입점에 `try/catch`. 예외를 사내 페이지로 전파시키지 않습니다.
- 스크립트 실행 로그를 남깁니다(콘솔, 접두어 통일).
- **긴급 OFF가 항상 가능해야 합니다.** 스크립트 상단 플래그 하나 또는 스크립트 비활성화만으로
  사내 페이지가 원래 상태로 돌아와야 합니다.
- 새 기능은 기존 기능과 **backward compatible**해야 합니다.

## 4. 데이터 판정 규칙

- 원본 상태값과 정규화 상태값을 **둘 다 유지**합니다. (`raw_status`, `status`)
- 같은 코드값이라도 화면/탭에 따라 의미가 다를 수 있으므로, **공통 코드표 하나로 뭉뚱그려 해석하지 않습니다.**
  해석은 해당 화면 담당 모듈에서 합니다.
- "할 일"로 판정한 건은 **판정 근거를 함께 보여줍니다.** (어떤 필드가 어떤 값이라서 잡혔는지)
- 판정 로직에는 fixture 기반 테스트를 붙입니다.

## 5. 브랜치 / 커밋

- `main` 직접 수정 금지. 항상 브랜치를 만듭니다.
  `feature/*` · `fix/*` · `refactor/*` · `docs/*`
- **한 번에 Issue 하나.** 요청받지 않은 리팩터링을 끼워 넣지 않습니다.
- 커밋 메시지: `타입(범위): 요약`
  ```
  feat(ipms): 업무요청 기한초과 카드 추가
  fix(mail): 응답이 비었을 때 발생하던 오류 수정
  refactor(stats): 조회 로직 분리
  docs: 스크립트 목록 갱신
  ```
- userscript 파일의 `// @version`은 기능이 바뀔 때마다 올립니다. (Tampermonkey 업데이트 판단 기준)

## 6. 변경 절차

1. 고치기 전에 **관련 기존 코드를 먼저 읽고** 현재 동작을 설명할 수 있어야 합니다.
2. 잘 돌아가는 스크립트를 "구조가 안 예쁘다"는 이유만으로 리팩터링하지 않습니다.
   손대는 김에 필요한 만큼만 정리합니다.
3. 변경 후에는 **영향 범위**를 적습니다. (바뀐 파일 / 깨질 수 있는 기존 동작 / 되돌리는 방법)
4. 내부망에서 실제로 확인하기 전까지 `main`에 넣지 않습니다.

## 7. 파일 배치

- userscript는 `userscripts/`에 **파일 1개 = 스크립트 1개**. 확장자는 `.user.js`.
- Python은 `python/`에 평평하게 둡니다. 한 프로그램이 여러 파일로 나뉘어 서로 import하기 시작하면
  **그 프로그램만** `python/<이름>/` 폴더로 승격합니다.
- 폴더를 미리 만들지 않습니다. 복잡해진 것만 나중에 승격합니다.

## 8. Tampermonkey ↔ Python 연계

- Python이 사내 시스템에 직접 로그인하지 않습니다. 인증은 브라우저 세션에 맡깁니다.
- 연계는 **파일 경유**가 기본입니다: Tampermonkey가 표준 JSON을 내보내고 → Python이 그 파일을 읽습니다.
- localhost 서버 방식은 회사 보안정책상 허용이 확인되기 전까지 쓰지 않습니다.
