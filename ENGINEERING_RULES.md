# ENGINEERING RULES

이 저장소에서 코드를 쓰는 모든 주체(나, Claude, GPT, Codex)가 공통으로 따르는 규칙이다.
`CLAUDE.md` · `AGENTS.md` · `state/brief/*.md` 는 전부 이 파일을 참조한다.
**규칙이 바뀌면 이 파일만 고친다.**

> 실측 근거는 [docs/FINDINGS.md](docs/FINDINGS.md)에 있다.
> "지킬 수 없는 규칙은 규칙이 아니다" — 이 문서의 이전 판에는 실제 코드가 0/17로 어기고 있던
> 조항과, 매일 돌고 있는 기능을 금지하는 조항이 있었다. 그런 조항은 삭제했다.

---

## 0. 커밋을 막는 것은 정확히 네 가지

이 목록이 다섯 개가 되는 날, 사용자는 저장소를 우회하기 시작한다. 늘리지 않는다.

1. **PII·자격증명** — `tools/guard.py`
2. **구문 파손** — `node --check`
3. **구조 계약 파손** — `@namespace` 변경 · 프리앰블 부재 · 인라인 마커 쌍 불균형
4. **쓰기가드 위반** — `contracts/write-guard.yaml` 밖의 쓰기 호출

나머지는 전부 **알림**이거나 **배포본 미갱신**이다(→ [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) 게이트).

검사기는 **침묵을 성공으로 읽지 않는다**: 검사 대상 0개는 실패, 읽을 수 없는 파일은 실패,
검사 도구가 없으면 건너뛰지 말고 그 사실을 말한다.

---

## 1. 보안 — 절대 commit 금지

- 실제 로그인 쿠키 / 세션 / 인증 헤더 / 비밀번호 / API 키
- 실제 사번·실명·연락처·사내 메일 주소
- 실제 특허·과제·비용 데이터, 실제 관리번호
- 사내 API의 **응답 dump**, 브라우저 HAR, 네트워크 캡처
- **화면 스크린샷** (마스킹을 검증할 방법이 없다)
- 회사 내부 문서 원본, 백업 zip

패턴 사전은 [`tools/rules/pii.txt`](tools/rules/pii.txt)에 있다. **코드가 아니라 데이터이므로
오탐이 나면 그 파일을 고친다.** `--no-verify`로 넘기지 않는다.

**저장소는 private 고정.** CI가 visibility를 확인해 public이면 실패시킨다.

익명화 fixture는 `tests/fixtures/sanitized/`에 둔다. 값만 바꾸고 **필드명과 자료구조는 실제와 같게** 유지한다.
비정상 케이스(빈 배열·`null`·예상 밖 상태코드)를 반드시 포함한다 — 그 입력에서 조용히 종료되는지가
이 저장소에서 가장 중요한 테스트다.

**내부 호스트명·화면 ID는 `WARN` 등급이다.** 제거 대상이 아니라 **반출 가능 여부를 사람이 판단할 대상**이다.
스크립트의 `@match`와 라우팅에 구조적으로 필요하기 때문이다. 판단이 끝나기 전까지는 경고로 남는다.

---

## 2. 사내 시스템에 대한 행동 제한

1. **조회(read) 우선.** 목록·상세 조회로 목적을 달성할 수 있으면 그렇게 한다.
2. **쓰기 API는 명시적 요청이 있을 때만.** 차단 목록의 유일한 원본은 `contracts/write-guard.yaml`이고,
   각 스크립트가 자기 정규식을 따로 갖지 않는다.
   (실측: 지금은 스크립트마다 달라서 접수·제출·발행·확인·취소 계열이 한쪽에서만 차단된다.)
3. **최종 실행 버튼은 사람이 누른다.** 값을 채워주는 것까지가 한계다.
4. **쓰기·승인 경로를 건드리는 스크립트는 킬스위치 없이 반입할 수 없다.**
   사람의 마지막 확인을 제거하는 코드에는 그것을 끄는 수단이 같은 파일 안에 있어야 한다.
5. **전역 네트워크 훅(fetch/XHR 교체)을 설치하는 스크립트는 평상시 비활성이 기본이다.**
   차단할 때는 가짜 성공(HTTP 200)이 아니라 명시적 실패를 돌려준다.
   그리고 호출하는 쪽은 HTTP 상태만 보지 말고 응답 봉투의 `errorCode`를 확인한다.

---

## 3. 방어적 코딩 — 기존 시스템을 깨뜨리지 않기

- **DOM 요소가 없으면 오류 없이 종료한다.**
- **시스템 기능을 덮어쓰지 않는다.** 기존 함수·이벤트를 교체하지 말고 추가한다.
- **응답이 예상과 다르면 기존 동작을 유지**하고 내 기능만 비활성화한다.
- 모든 진입점에 `try/catch`. 예외를 사내 페이지로 전파시키지 않는다.
- 로그 접두어에 slug를 포함한다.

### 킬스위치 프로토콜 (모든 스크립트 필수)

파일 맨 위 프리앰블 한 블록이 세 가지를 동시에 한다 — 자기등록 · 전역 킬스위치 수신 · 중복 인스턴스 방지.

```js
/* WAP-ID START (자동 생성 · 손대지 마세요) */
var WAP_ID = 'mail-gwx', WAP_VER = '1.3.8', WAP_BASE = 'a1b2c3d';
try {
  if (localStorage.getItem('wap.kill') === '1') return;
  if (window['__wap_' + WAP_ID]) return;              // 두 벌 설치 방지
  window['__wap_' + WAP_ID] = WAP_VER;
  (window.__WAP = window.__WAP || []).push({ id: WAP_ID, ver: WAP_VER });
  localStorage.setItem('wap:installed:' + WAP_ID, WAP_VER);
} catch (e) {}
/* WAP-ID END */
```

- `localStorage`를 쓰는 이유: GM 저장소는 스크립트별로 격리되어 **전역 킬이 불가능**하다.
- 삽입하는 모든 요소에 **공통 표식 `data-wap="<slug>"`** 를 붙인다. 접두어를 스크립트마다 만들지 않는다.
  (실측: 지금 9종으로 갈라져 있어 일괄 제거가 불가능하다.)
- CSS 변경은 한 클래스 아래에 격리해 클래스 제거만으로 원복되게 한다.

> 이전 판의 `const ENABLED = true` 규범은 삭제했다. 17개 중 0개가 지키고 있었고,
> 스크립트별 플래그로는 브리프가 요구한 "전체 비활성화"가 성립하지 않는다.

---

## 4. 데이터 판정

- **원본 상태값과 정규화 상태값을 둘 다 유지한다.**
- **상태코드를 전역 공통표로 해석하지 않는다.** 같은 코드가 화면마다 다른 뜻이다.
  해석의 유일한 원본은 `contracts/status-codes.yaml`이고 키는 (화면ID, 필드명, 코드)다.
  상태 필드 **이름 자체도** 화면마다 다르므로 계약 항목이다.
- 계약의 모든 레코드는 `confidence: measured | inferred | unknown | conflict` 를 갖는다.
  `conflict`는 정상 상태다 — 여러 출처가 다를 때 **해결하지 말고 드러낸다.**
- "할 일"로 판정한 건은 **판정 근거를 함께 보여준다.**
- 판정 로직에는 fixture 기반 테스트를 붙인다.

---

## 5. 정체성 · 버전 · 브랜치

- **정체성 키는 slug다.** 파일명도 `@name`도 아니다. 원본은 `contracts/scripts.yaml`.
- **`@namespace`는 `wap.<slug>`로 고정하고 그 뒤로 바꾸지 않는다.** 바꾸면 Tampermonkey가
  별개 스크립트로 보고 GM 데이터가 갈라진다.
- **새 버전은 Tampermonkey 편집기에서 덮어쓴다.** "새 스크립트 설치"로 넣지 않는다.
- **잃으면 곤란한 상태는 `localStorage`/IndexedDB에 `wap:<slug>:<name>` 키로 둔다.**
  `GM_setValue`는 지워져도 되는 토글에만 쓴다.
- `@version`은 **SemVer 3자리**(`1.4.2`)로 고정한다. 기능이 바뀌면 올린다.
- **`@description`에 변경이력을 쌓지 않는다.** `changelogs/<slug>.md`로 간다.
- `main` 직접 수정 금지. `feature/*` `fix/*` `refactor/*` `docs/*`.
  단 **`inbox/` 반입은 예외로 `main` 직접 커밋을 허용한다** — 여기에 마찰을 두면 우회가 시작된다.
- 커밋 메시지: `타입(범위): 요약`

---

## 6. 외부 의존

- **`@require`로 외부 CDN을 불러오지 않는다.** 내부망에서 접근 가능한지 보장되지 않고,
  버전이 고정되지 않으면 파싱 결과가 조용히 달라진다.
- 서드파티 라이브러리는 `vendor/`에 **버전을 고정해 커밋**하고 SHA256을 `vendor/CHECKSUMS.txt`에 남긴다.
- 라이브러리를 `localStorage`에 넣고 `eval`하지 않는다. 브라우저 프로필이 초기화되면 기능이 통째로 정지한다.
- 호스트 페이지의 라이브러리(jQuery·Kendo 등)에 의존하는 곳은 문서화하고, 없을 때 조용히 종료한다.

---

## 7. 파일 배치

- 스크립트는 `scripts/<slug>.user.js`. **경로는 slug가 정하므로 사람이 파일명을 지을 일이 없다.**
- 반입은 `inbox/`에 아무 이름으로 넣는 것으로 끝난다.
- Python은 `python/` 아래에 평평하게. 한 프로그램이 여러 파일로 커지면 그것만 폴더로 승격한다.
- `state/`는 전부 생성물이다. 사람이 손대지 않고 리뷰하지 않는다.
- `twin/raw/` · `recon/raw/`는 **커밋 금지**. 마스킹을 통과한 것만 `twin/screens/`로 간다.

---

## 8. Tampermonkey ↔ Python 연계

**데이터 흐름은 Python이 만들고 Tampermonkey가 읽는 쪽이다.** (이전 판은 반대로 적혀 있었다.)

| 등급 | 방식 | 규칙 |
|---|---|---|
| 1순위 | **결과 폴더 경유** (File System Access API) | 서버 없음. 기본 경로 |
| 2순위 | 다운로드 폴더에 놓고 읽기 | 수동 |
| 3순위 | **localhost 브리지** (`127.0.0.1:8765`) | 기존 것은 유지. **신규 확장만 사전 확인** |

- `jobs[]` 스키마의 유일한 원본은 `contracts/bridge/manifest.schema.json`이다.
- 다른 스크립트가 Python 산출물을 받을 때 **새 통신 방식을 만들지 않는다.** 같은 manifest 형태를 쓴다.
- Python이 사내 시스템에 직접 로그인하지 않는다. 인증은 브라우저 세션에 맡긴다.

---

## 9. 통계는 어디서 계산하는가

| 웹(Tampermonkey) | Python |
|---|---|
| 한 화면·한 API로 끝나는 집계, 건수·합계·D-day | 여러 소스 매칭, 퍼지 매칭, 다중 시트 |
| 목록 표시, CSV·클립보드 내보내기 | 비용 대사, 연도별 보고서, 서식 있는 xlsx |
| **원자료를 내보내는 것까지** | **원자료를 받아 가공하는 것부터** |

**브라우저에서 xlsx를 굽지 않는다.** 그것이 외부 CDN 의존을 만든 원인이다.
분류 규칙(적요 정규식·단계 매핑)은 `rules/*.json` 한 벌을 JS와 Python이 함께 읽는다.

---

## 10. 변경 절차

1. 고치기 전에 **관련 기존 코드를 먼저 읽고** 현재 동작을 설명할 수 있어야 한다.
   거대 파일은 `state/outline/<slug>.md`로 접근한다.
2. 잘 돌아가는 스크립트를 구조 때문에 갈아엎지 않는다.
3. 변경 후 **영향 범위**를 적는다 — 바뀐 파일 / 깨질 수 있는 기존 동작 / 되돌리는 방법.
4. **내부망에서 사람이 확인하기 전까지 `main`에 넣지 않는다.**
   두 AI 모두 사내망을 볼 수 없다. "테스트 통과"는 코드가 말이 된다는 뜻이지 동작한다는 뜻이 아니다.
