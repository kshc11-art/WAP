# WAP 공통 작업 규칙

사용자의 현재 요청이 작업 범위다. 각 프로그램은 독립적으로 개선한다. 구조 정리를 이유로 통합·전면 재작성하지 않는다.

- 작업 전 해당 코드, README 색인, 지식파일의 관련 항목과 최신 정정 내용을 읽는다. 미확정 사실은 미확정으로 남긴다.
- GitHub는 개발 기준이고 회사 PC의 실행 버전은 별도다. 회사에서 확인하지 않은 동작을 확인 완료라고 하지 않는다.
- 사용자가 요청한 프로그램과 관련 테스트를 수정한다. source 변경 시 validation/catalog.json의 현재 sha256·버전도 갱신하고 원본 출처는 보존한다.
- scripts의 @name/@namespace/저장 키를 정리 목적으로 바꾸지 않는다. 외부 CDN을 새로 도입하지 않는다. 기존 동작과 사내 페이지의 기능을 보존한다.
- 통계 변경은 행별 기대 결과, 키 유일성, 입력·출력 건수, 기준일 경계, 누락·중복·충돌을 검증한다. 총합 일치만으로 성공을 판정하지 않는다.
- 사용자가 일부러 제공하지 않은 로직·원자료는 범위 밖이다. 임의로 만들거나 golden을 추정하지 않는다.
- raw는 입력 원본, golden은 사용자가 기준으로 지정한 기대 결과, actual은 프로그램 새 실행 결과다. actual을 자동으로 golden에 복사하지 않는다. 기준 변경은 사용자의 명시적 요청과 근거를 남긴다.
- 기본 실데이터 위치는 validation/raw와 validation/golden이며 Git 추적에서 제외한다. 추적은 빈 폴더와 비교 설정만 한다. 실데이터 Git 업로드는 별도 명시적 요청이 필요하다.
- 사용자가 제공하고 반입을 요청한 기존 소스·지식은 이번 비공개 저장소의 초기 기준본이다. 여기에 이미 포함된 업무 상수·예외를 정리 과정에서 임의로 제거하지 않는다. 새 운영 데이터·쿠키·인증정보를 코드에 추가하지 않는다.
- 로컬 검사는 python tools/check.py 와 python -m unittest discover -s tests -v. python validation/diagnostics.py는 현재 알려진 결함을 재현하며 미해결이면 종료코드 1이다.
- python validation/run.py compare <id> 는 제공된 raw/golden/actual 및 해시를 검사한다. 자료 미제공은 WAITING(2), 불일치는 FAIL(1), 실제 비교 일치만 PASS(0)다.
- main 반영은 변경 범위에 맞는 검사를 통과한 뒤 한다. 프로그램 동작 변경은 내부망/Office 확인 항목을 함께 남긴다. 단순 반입은 배포·활성화가 아니다.
- 한 작업마다 별도 설계서·최종본 복사·인수인계 파일을 만들지 않는다. README, catalog, 해당 테스트와 Git 기록을 갱신한다.

2026-09-14: 사용자의 독립 프로그램 정리 및 지식/raw/golden 관리 요청에 따라 기존 '저장소는 사본 전용·Claude는 scripts 수정 제한·자동 반입 봇' 운영안을 대체했다. 이전 문서와 규칙은 Git 이력에 보존된다.

- catalog.json의 business_pattern_baseline은 최초 반입 코드에 이미 존재한 업무 문자열의 지문이다. 일반 편집 중 갱신하지 않는다. 새 인증정보에는 예외가 없다. 위원회의 실제 자료가 채워진 서식·부서 매핑·구형 golden은 원본 ZIP에서 로컬로 복원한다.
